services:
  stratum-ts-mongo:
    image: mongo:latest
    container_name: stratum-ts-mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=m11615
      - MONGO_INITDB_DATABASE=stratum
    volumes:
      - stratum-ts-mongo-volume:/data/db
      - ../initialisation/mongo:/docker-entrypoint-initdb.d
    networks:
      - stratum-ts-network
    restart: no

  stratum-ts-server:
    build:
      context: ../..
      dockerfile: ./server/Dockerfile
    container_name: stratum-ts-server
    ports:
      - "3000:3000"
    networks:
      - stratum-ts-network
    depends_on:
      - stratum-ts-mongo
    restart: no

  stratum-ts-client:
    build:
      context: ../..
      dockerfile: ./client/Dockerfile
    container_name: stratum-ts-client
    ports:
      - "3001:3001"
    networks:
      - stratum-ts-network
    extra_hosts:
      - "server:host-gateway"
    depends_on:
      - stratum-ts-server
    restart: no

volumes:
  stratum-ts-mongo-volume:
    driver: local

networks:
  stratum-ts-network:
    driver: bridge
